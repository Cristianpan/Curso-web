// Numeros

/**
 * Solo existe un tipo de dato para los numeros
 * el number. No es necesario especificar el tipo de numero
 */

const num1 = 100; 
const num2 = 300; 

const sum = num1 + num2; 
const resta = num2 - num1; 
const producto = num1 * num2; 
const cociente = num2 / num1; 
const residuo = num1 % num2; 

console.log(sum); 
console.log(resta); 
console.log(producto); 
console.log(cociente); 
console.log(residuo); 

